# 배포 사고 기록 및 재발방지 체크리스트

작성일: 2026-09-22

---

## 0. 무슨 일이 있었나 (한 줄 요약)

**PC에서 작업한 내용이 몇 주치 쌓여 있었는데, git에 커밋이 안 되고 있었습니다.** 그래서 사장님 컴퓨터에서 보이는 화면(로그인 디자인, 3선 메뉴, 카드입금예정 등)과 실제 라이브 사이트(Vercel)가 서로 다른 버전이 되어 있었습니다. 코드가 손상되거나 배포가 잘못된 게 아니라, **"수정 → 로컬 저장" 다음 단계인 "git commit → push"가 누락된 파일이 73개(핵심 페이지 26개 + 한 번도 커밋 안 된 신규 파일 47개)** 쌓여 있었던 것입니다.

---

## 1. 왜 이런 일이 생겼는가

- git은 `git add` → `git commit` → `git push` 세 단계를 다 거쳐야 실제 배포(Vercel)에 반영됩니다. 파일을 저장(Ctrl+S)한 것과 git에 반영된 것은 완전히 별개입니다.
- 이 프로젝트는 여러 세션(Claude, Codex 등)에 걸쳐 오래 작업해왔는데, 작업 세션이 끝날 때마다 커밋하는 습관이 지켜지지 않았습니다.
- `git status`를 자주 확인하지 않으면 "로컬엔 있는데 git엔 없는" 파일이 조용히 쌓입니다. 화면상으로는 정상 작동하니 문제를 못 느끼다가, 배포된 라이브 사이트를 열어봐야만 차이가 드러납니다.
- 게다가 `.docx` 중복 파일, 로그 파일 등 실제 코드가 아닌 잡파일들이 같이 안 올라가 있다 보니, `git status` 결과가 너무 길고 지저분해서 "진짜 중요한 미반영 파일"을 눈으로 찾기 어려웠습니다.

---

## 2. 오늘 실제로 있었던 구체적 사고 3건

### ① settlement.html(카드입금예정) 404
- 원인: Vercel 빌드 스크립트(`scripts/build-vercel-static.mjs`)의 **로컬 버전**은 settlement.html을 포함하도록 이미 수정돼 있었지만, **git에 커밋된 버전**은 그 이전의 구식 스크립트였음.
- 조치: 로컬의 최신 스크립트를 커밋 → 반영 완료.

### ② 로그인 화면 · 3선 메뉴가 "예전 버전"으로 보임
- 원인: `login.html`, `header.js`, `index.html` 등 핵심 파일 자체가 로컬에서 수정된 채 **한 번도 git에 올라간 적이 없었음**. 라이브는 몇 주 전 상태 그대로였음.
- 조치: 미반영 파일 전체를 확인(`git status`) 후 일괄 커밋+푸시.

### ③ (가장 주의할 사고) 카카오 로그인이 갑자기 안 됨
- 원인: 로그인 화면 UI를 새로 반영하는 과정에서, 그 로컬 파일 자체가 **카카오 KOE205 문제를 해결했던 이전 수정(`kakao-auth` Edge Function 우회 코드)보다 더 오래된 버전에서 시작된 파일**이었음. 그래서 "로컬 = 항상 최신"이라는 가정으로 그대로 덮어썼더니, UI는 새로워졌지만 카카오 로그인 기능은 오히려 퇴보함.
- 조치: `KAKAO_FN` 우회 로직을 다시 추가해서 복구.
- **이게 가장 중요한 교훈입니다** → 3번 항목 참고.

---

## 3. 재발방지 체크리스트 (중요)

### ★ 작업을 마칠 때마다 (매번)

1. 터미널에서 `git status` 실행 — "Changes not staged" / "Untracked files"에 뭔가 있으면 그날 안에 정리한다. 며칠 이상 방치하지 않는다.
2. 의미 있는 수정이 끝났으면 바로 `git add` → `git commit` → `git push`까지 그 자리에서 끝낸다. "나중에 한꺼번에 올려야지"가 오늘 사고의 근본 원인이었다.

### ★ 로컬 파일을 git에 올리기 전에 (특히 오래 묵혀둔 파일일수록)

3. `git diff HEAD -- 파일명`으로 **실제로 뭐가 바뀌는지 먼저 읽어본다.** 특히 로그인/결제/인증 관련 파일은 "로컬이 최신이니까 무조건 좋아진 것"이라고 가정하지 않는다 — 이번 카카오 사고처럼, 로컬 파일이 화면 디자인은 최신이어도 기능적으로는 더 예전 로직을 담고 있을 수 있다.
4. 소셜 로그인처럼 눈에 안 보이는 기능은 **UI만 확인하지 말고 실제로 버튼을 눌러서** 구글/카카오/네이버 각각 로그인이 되는지 직접 테스트한다.

### ★ 배포 후 확인

5. 푸시 후에는 라이브 사이트(https://meatos.vercel.app)를 **직접 열어서** 눈으로 확인한다. Vercel Deployments 탭에서 최신 커밋이 "Production"으로 표시되는지도 같이 확인한다.
6. 캐시(브라우저/PWA) 탓으로 돌리기 전에, 먼저 코드/git 쪽 증거(커밋 여부, diff)부터 확인한다 — 이번 사고들은 전부 캐시가 아니라 미커밋 문제였다.

### ★ 관리자 권한(POS 에이전트) 관련, 별도로 챙길 것

7. `MeatosPosSetup.cs` 같은 C# 소스코드는 **컴파일해서 나온 설치 파일(.exe)을 `downloads/` 폴더에 새로 넣고 다시 커밋**해야 실제로 배포됩니다. 소스코드만 고치고 끝내면 사장님들이 받는 설치 파일은 여전히 예전 버전입니다. (오늘 downloads/ 폴더 자체가 git에 없었던 것도 이번에 같이 확인·반영했습니다.)

### ★ 저장소 정리 (급하지 않음, 여유 될 때)

8. `.docx` 중복 파일들, `.build/`·`artifacts/`·`output/` 폴더, `supabase/.temp/` 등은 `.gitignore`에 등록해서 `git status`가 항상 깔끔하게 나오도록 정리하면, 다음부터 "진짜 안 올라간 파일"을 훨씬 빨리 찾을 수 있습니다.

---

## 4. 오늘 최종적으로 반영된 것 (커밋 이력)

- `a898de4` — 배포 빌드 스크립트에 settlement.html 포함
- `c806715` — 로그인 화면 간편로그인 버튼 위치/아이콘 최신화 (+ 이후 카카오 우회 복원 포함)
- `24c5cfd` — 미커밋 상태였던 핵심 페이지 26개 + 신규 파일 47개(계정연결/배달연동/안전재고/자동발주 페이지, 엔진 스크립트, POS 설치 파일, 카카오·네이버 로그인 Edge Function) 일괄 반영
